# Server configuration
server.port=8080

# CORS configuration to allow frontend access from Vite dev server
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Logging configuration
logging.level.org.springframework.web=INFO
logging.level.com.example=DEBUG

# Datasource configuration (example placeholders; customize as needed)
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver

# Additional recommended Spring Boot properties can be added here as necessary