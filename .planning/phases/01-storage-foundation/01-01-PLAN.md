---
phase: 01-storage-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [src/storage.js, index.html]
autonomous: true

# Feature traceability (link to original requirements)
features:
  implements: ["TASK-05"]  # Storage foundation
  depends_on: []
  enables: ["TASK-01", "TASK-02", "TASK-03", "TASK-04"]  # CRUD operations in Phase 2

must_haves:
  truths:
    - "Storage module can save tasks without error"
    - "Storage module can load tasks without error"
    - "HTML page loads in browser without JavaScript errors"
    - "LocalStorage persists data across page reloads"
  artifacts:
    - path: "src/storage.js"
      provides: "LocalStorage wrapper with save/load functions"
      exports: ["saveTasks", "loadTasks", "isStorageAvailable", "generateTaskId"]
      min_lines: 30
    - path: "index.html"
      provides: "Main HTML file with empty task list"
      contains: "task-list element"
  key_links:
    - from: "index.html"
      to: "src/storage.js"
      via: "script tag import"
      pattern: "script.*src.*storage\\.js"
---

<objective>
Create storage foundation: LocalStorage utility module and HTML file with empty task list.

Purpose: Build the persistence layer so tasks can survive browser sessions. This is the foundation all subsequent CRUD operations depend on.

Output: src/storage.js (storage utilities), index.html (entry point)
</objective>

<feature_dependencies>
Implements: TASK-05: Storage foundation for task persistence
Depends on: None (Phase 1 - no dependencies)
Enables: TASK-01, TASK-02, TASK-03, TASK-04 (CRUD operations in Phase 2)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-storage-foundation/01-RESEARCH.md
@.planning/phases/01-storage-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LocalStorage utility module</name>
  <files>src/storage.js</files>
  <action>
Create src/storage.js with storage utilities.

Requirements from user decisions:
- Storage key: `todo-app-tasks`
- Data format: `[{id: "uuid", title: "string", completed: boolean, created: timestamp}]`
- Error handling: Catch quota errors with graceful fallback

Implement these functions:
1. `isStorageAvailable()` - Test localStorage with set/remove pattern, returns boolean
2. `generateTaskId()` - Use crypto.randomUUID() (with fallback for non-secure context)
3. `loadTasks()` - Get from localStorage, JSON.parse with try-catch, return [] on error
4. `saveTasks(tasks)` - JSON.stringify with try-catch, handle QuotaExceededError specifically

Use the exact code patterns from 01-RESEARCH.md sections "Complete Storage Module Pattern" and "Storage Availability Check".
  </action>
  <verify>
Verify file exists and contains all required exports:
- grep -q "export.*isStorageAvailable" src/storage.js
- grep -q "export.*loadTasks" src/storage.js
- grep -q "export.*saveTasks" src/storage.js
- grep -q "export.*generateTaskId" src/storage.js
- grep -q "QuotaExceededError" src/storage.js
  </verify>
  <done>
storage.js exports: isStorageAvailable, loadTasks, saveTasks, generateTaskId. All functions have try-catch error handling. Handles QuotaExceededError gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HTML file with empty task list</name>
  <files>index.html</files>
  <action>
Create index.html as the main entry point.

Requirements:
- Basic HTML5 structure with doctype
- Imports src/storage.js via script tag
- Contains an empty task list container (id="task-list")
- Shows empty state message when no tasks exist

Structure:
- Header: "My Tasks" title
- Task list container: ul#task-list
- Empty state: "No tasks yet" message (hidden when tasks exist)

Use simple, semantic HTML. No CSS framework - keep styling minimal for now.

Import storage module at the end of body, then load initial tasks to verify storage works.
  </action>
  <verify>
Verify structure:
- grep -q 'id="task-list"' index.html
- grep -q 'src/storage.js' index.html
- Verify HTML is valid: no unclosed tags
  </verify>
  <done>
index.html loads in browser with empty task list, no JavaScript errors in console. Page renders without crash.
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] src/storage.js exports all 4 functions
- [ ] src/storage.js handles QuotaExceededError gracefully
- [ ] src/storage.js handles JSON.parse errors gracefully
- [ ] index.html exists and loads without errors
- [ ] index.html imports storage.js correctly
- [ ] Browser can load tasks from storage
- [ ] Browser can save tasks without error
</verification>

<success_criteria>
Measurable completion:
1. Opening index.html in a browser displays an empty task list with no JavaScript errors in console
2. Calling loadTasks() returns [] (empty array) when no tasks saved
3. Calling saveTasks([{id: "test", title: "Test", completed: false, created: 123}]) returns {success: true}
4. Page refresh shows tasks persisted
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-foundation/01-01-SUMMARY.md`
</output>